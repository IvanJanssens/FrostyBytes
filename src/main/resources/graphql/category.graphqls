type Category {
    id: ID!
    name: String!
    icon: String!
}

extend type Subscription {
    getCategories:[Category]!
}

extend type Mutation {
    addCategory(name: String!, icon: String!): ID
    updateCategory(id: ID!, name: String, icon: String!): Category
    deleteCategory(id: ID!): ID
}
